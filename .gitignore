# Personal files (resume, etc.) — not for the public portfolio
/anita-folder
/computer-systems-notes

# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.env

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.ipynb_checkpoints/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Written by the site's CI job so the documented browser test count can be
# checked against what vitest actually ran.
vitest.json
htmlcov/

# vitest --reporter=json output, written by the curve-lab API check in CI.
api-tests.json
